//
//  Frends.swift
//  MessangerVK
//
//  Created by Оксана Каменчук on 22.02.2022.
//

import UIKit

struct Friends {
    let friendName: String
    let friendAvatar: String
    //let friendPhotos: [UIImage?]
}
/*
class Friends {
    
    let image: UIImage?
    let name: String
    
    init(image: UIImage? = nil, name: String) {
        self.image = image
        self.name = name
    }
    
    class func generateFriendsArray() -> [Friends] {
        
        var friendsArray = [Friends]()
        
        friendsArray.append(Friends(image: UIImage.init(named: "mummi"), name: "Муми-троль"))
        friendsArray.append(Friends(image: UIImage.init(named: "mummimama"), name: "Муми-мама"))
        friendsArray.append(Friends(image: UIImage.init(named: "moominpapa"), name: "Муми-папа"))
        friendsArray.append(Friends(image: UIImage.init(named: "snu"), name: "Снусмумрик"))
        friendsArray.append(Friends(image: UIImage.init(named: "frekensnork"), name: "Фрекен Снорк"))
        friendsArray.append(Friends(image: UIImage.init(named: "sniff"), name: "Снифф"))
        friendsArray.append(Friends(image: UIImage.init(named: "hemul1"), name: "Хемуль"))
        friendsArray.append(Friends(image: UIImage.init(named: "mumla1"), name: "Мюмла"))
        
        return friendsArray
    }
}
*/
